console.clear()

const palavras = ["Verde", "Rosa", "Vermelho", "Roxo"]

let i = 0
while (i < palavras.length) {
    if (palavras[i].length > 4) {
        console.log (palavras[i])
    }
    i++
}

