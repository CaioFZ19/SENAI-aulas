const texto = true

const decisao = texto = true ? "Atrubuição por ternário: Concluída com sucesso." : "FALHA"

if (decisao == texto) {
    console.log (" Procedimento complexo de checagem de logs ativado...")
} else {
    console.log ("FALHA")
}
